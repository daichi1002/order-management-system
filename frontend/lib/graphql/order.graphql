mutation CreateOrder($input: OrderInput!) {
  createOrder(input: $input)
}

mutation CancelOrder($id: ID!) {
  cancelOrder(id: $id)
}

query GetOrders {
  getOrders {
    id
    items {
      name
      quantity
      price
    }
    totalAmount
    ticketNumber
    createdAt
  }
}
